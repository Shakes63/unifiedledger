import { getDatabaseDialectFromUrl } from "./dialect";
import * as sqlite from "./schema.sqlite";
import * as pg from "./schema.pg";

const dialect = getDatabaseDialectFromUrl(process.env.DATABASE_URL);

export const accountBalanceHistory = dialect === "postgresql" ? pg.accountBalanceHistory : sqlite.accountBalanceHistory;
export const accountBalanceHistoryRelations = dialect === "postgresql" ? pg.accountBalanceHistoryRelations : sqlite.accountBalanceHistoryRelations;
export const accountDeletionRequests = dialect === "postgresql" ? pg.accountDeletionRequests : sqlite.accountDeletionRequests;
export const accounts = dialect === "postgresql" ? pg.accounts : sqlite.accounts;
export const accountsRelations = dialect === "postgresql" ? pg.accountsRelations : sqlite.accountsRelations;
export const activityLog = dialect === "postgresql" ? pg.activityLog : sqlite.activityLog;
export const backupHistory = dialect === "postgresql" ? pg.backupHistory : sqlite.backupHistory;
export const backupSettings = dialect === "postgresql" ? pg.backupSettings : sqlite.backupSettings;
export const billInstances = dialect === "postgresql" ? pg.billInstances : sqlite.billInstances;
export const billInstancesRelations = dialect === "postgresql" ? pg.billInstancesRelations : sqlite.billInstancesRelations;
export const billMilestones = dialect === "postgresql" ? pg.billMilestones : sqlite.billMilestones;
export const billPayments = dialect === "postgresql" ? pg.billPayments : sqlite.billPayments;
export const bills = dialect === "postgresql" ? pg.bills : sqlite.bills;
export const billsRelations = dialect === "postgresql" ? pg.billsRelations : sqlite.billsRelations;
export const budgetCategories = dialect === "postgresql" ? pg.budgetCategories : sqlite.budgetCategories;
export const budgetCategoriesRelations = dialect === "postgresql" ? pg.budgetCategoriesRelations : sqlite.budgetCategoriesRelations;
export const budgetPeriods = dialect === "postgresql" ? pg.budgetPeriods : sqlite.budgetPeriods;
export const budgetRolloverHistory = dialect === "postgresql" ? pg.budgetRolloverHistory : sqlite.budgetRolloverHistory;
export const calendarConnections = dialect === "postgresql" ? pg.calendarConnections : sqlite.calendarConnections;
export const calendarConnectionsRelations = dialect === "postgresql" ? pg.calendarConnectionsRelations : sqlite.calendarConnectionsRelations;
export const calendarEvents = dialect === "postgresql" ? pg.calendarEvents : sqlite.calendarEvents;
export const calendarEventsRelations = dialect === "postgresql" ? pg.calendarEventsRelations : sqlite.calendarEventsRelations;
export const calendarSyncSettings = dialect === "postgresql" ? pg.calendarSyncSettings : sqlite.calendarSyncSettings;
export const categorizationRules = dialect === "postgresql" ? pg.categorizationRules : sqlite.categorizationRules;
export const categorizationRulesRelations = dialect === "postgresql" ? pg.categorizationRulesRelations : sqlite.categorizationRulesRelations;
export const categoryTaxMappings = dialect === "postgresql" ? pg.categoryTaxMappings : sqlite.categoryTaxMappings;
export const creditLimitHistory = dialect === "postgresql" ? pg.creditLimitHistory : sqlite.creditLimitHistory;
export const creditLimitHistoryRelations = dialect === "postgresql" ? pg.creditLimitHistoryRelations : sqlite.creditLimitHistoryRelations;
export const customFieldValues = dialect === "postgresql" ? pg.customFieldValues : sqlite.customFieldValues;
export const customFieldValuesRelations = dialect === "postgresql" ? pg.customFieldValuesRelations : sqlite.customFieldValuesRelations;
export const customFields = dialect === "postgresql" ? pg.customFields : sqlite.customFields;
export const customFieldsRelations = dialect === "postgresql" ? pg.customFieldsRelations : sqlite.customFieldsRelations;
export const dataExportRequests = dialect === "postgresql" ? pg.dataExportRequests : sqlite.dataExportRequests;
export const debtPayments = dialect === "postgresql" ? pg.debtPayments : sqlite.debtPayments;
export const debtPaymentsRelations = dialect === "postgresql" ? pg.debtPaymentsRelations : sqlite.debtPaymentsRelations;
export const debtPayoffMilestones = dialect === "postgresql" ? pg.debtPayoffMilestones : sqlite.debtPayoffMilestones;
export const debtPayoffMilestonesRelations = dialect === "postgresql" ? pg.debtPayoffMilestonesRelations : sqlite.debtPayoffMilestonesRelations;
export const debtSettings = dialect === "postgresql" ? pg.debtSettings : sqlite.debtSettings;
export const debts = dialect === "postgresql" ? pg.debts : sqlite.debts;
export const debtsRelations = dialect === "postgresql" ? pg.debtsRelations : sqlite.debtsRelations;
export const householdActivityLog = dialect === "postgresql" ? pg.householdActivityLog : sqlite.householdActivityLog;
export const householdInvitations = dialect === "postgresql" ? pg.householdInvitations : sqlite.householdInvitations;
export const householdMembers = dialect === "postgresql" ? pg.householdMembers : sqlite.householdMembers;
export const householdMembersRelations = dialect === "postgresql" ? pg.householdMembersRelations : sqlite.householdMembersRelations;
export const householdSettings = dialect === "postgresql" ? pg.householdSettings : sqlite.householdSettings;
export const households = dialect === "postgresql" ? pg.households : sqlite.households;
export const householdsRelations = dialect === "postgresql" ? pg.householdsRelations : sqlite.householdsRelations;
export const importHistory = dialect === "postgresql" ? pg.importHistory : sqlite.importHistory;
export const importHistoryRelations = dialect === "postgresql" ? pg.importHistoryRelations : sqlite.importHistoryRelations;
export const importStaging = dialect === "postgresql" ? pg.importStaging : sqlite.importStaging;
export const importStagingRelations = dialect === "postgresql" ? pg.importStagingRelations : sqlite.importStagingRelations;
export const importTemplates = dialect === "postgresql" ? pg.importTemplates : sqlite.importTemplates;
export const importTemplatesRelations = dialect === "postgresql" ? pg.importTemplatesRelations : sqlite.importTemplatesRelations;
export const interestDeductions = dialect === "postgresql" ? pg.interestDeductions : sqlite.interestDeductions;
export const merchants = dialect === "postgresql" ? pg.merchants : sqlite.merchants;
export const merchantsRelations = dialect === "postgresql" ? pg.merchantsRelations : sqlite.merchantsRelations;
export const nonMonthlyBills = dialect === "postgresql" ? pg.nonMonthlyBills : sqlite.nonMonthlyBills;
export const notificationPreferences = dialect === "postgresql" ? pg.notificationPreferences : sqlite.notificationPreferences;
export const notifications = dialect === "postgresql" ? pg.notifications : sqlite.notifications;
export const notificationsRelations = dialect === "postgresql" ? pg.notificationsRelations : sqlite.notificationsRelations;
export const oauthSettings = dialect === "postgresql" ? pg.oauthSettings : sqlite.oauthSettings;
export const pushSubscriptions = dialect === "postgresql" ? pg.pushSubscriptions : sqlite.pushSubscriptions;
export const quarterlyFilingRecords = dialect === "postgresql" ? pg.quarterlyFilingRecords : sqlite.quarterlyFilingRecords;
export const resourcePermissions = dialect === "postgresql" ? pg.resourcePermissions : sqlite.resourcePermissions;
export const ruleExecutionLog = dialect === "postgresql" ? pg.ruleExecutionLog : sqlite.ruleExecutionLog;
export const ruleExecutionLogRelations = dialect === "postgresql" ? pg.ruleExecutionLogRelations : sqlite.ruleExecutionLogRelations;
export const salesTaxCategories = dialect === "postgresql" ? pg.salesTaxCategories : sqlite.salesTaxCategories;
export const salesTaxSettings = dialect === "postgresql" ? pg.salesTaxSettings : sqlite.salesTaxSettings;
export const salesTaxTransactions = dialect === "postgresql" ? pg.salesTaxTransactions : sqlite.salesTaxTransactions;
export const savedSearchFilters = dialect === "postgresql" ? pg.savedSearchFilters : sqlite.savedSearchFilters;
export const savedSearchFiltersRelations = dialect === "postgresql" ? pg.savedSearchFiltersRelations : sqlite.savedSearchFiltersRelations;
export const savingsGoalContributions = dialect === "postgresql" ? pg.savingsGoalContributions : sqlite.savingsGoalContributions;
export const savingsGoalContributionsRelations = dialect === "postgresql" ? pg.savingsGoalContributionsRelations : sqlite.savingsGoalContributionsRelations;
export const savingsGoals = dialect === "postgresql" ? pg.savingsGoals : sqlite.savingsGoals;
export const savingsGoalsRelations = dialect === "postgresql" ? pg.savingsGoalsRelations : sqlite.savingsGoalsRelations;
export const savingsMilestones = dialect === "postgresql" ? pg.savingsMilestones : sqlite.savingsMilestones;
export const savingsMilestonesRelations = dialect === "postgresql" ? pg.savingsMilestonesRelations : sqlite.savingsMilestonesRelations;
export const searchHistory = dialect === "postgresql" ? pg.searchHistory : sqlite.searchHistory;
export const searchHistoryRelations = dialect === "postgresql" ? pg.searchHistoryRelations : sqlite.searchHistoryRelations;
export const tags = dialect === "postgresql" ? pg.tags : sqlite.tags;
export const tagsRelations = dialect === "postgresql" ? pg.tagsRelations : sqlite.tagsRelations;
export const taxCategories = dialect === "postgresql" ? pg.taxCategories : sqlite.taxCategories;
export const transactionAuditLog = dialect === "postgresql" ? pg.transactionAuditLog : sqlite.transactionAuditLog;
export const transactionAuditLogRelations = dialect === "postgresql" ? pg.transactionAuditLogRelations : sqlite.transactionAuditLogRelations;
export const transactionSplits = dialect === "postgresql" ? pg.transactionSplits : sqlite.transactionSplits;
export const transactionSplitsRelations = dialect === "postgresql" ? pg.transactionSplitsRelations : sqlite.transactionSplitsRelations;
export const transactionTags = dialect === "postgresql" ? pg.transactionTags : sqlite.transactionTags;
export const transactionTagsRelations = dialect === "postgresql" ? pg.transactionTagsRelations : sqlite.transactionTagsRelations;
export const transactionTaxClassifications = dialect === "postgresql" ? pg.transactionTaxClassifications : sqlite.transactionTaxClassifications;
export const transactionTemplates = dialect === "postgresql" ? pg.transactionTemplates : sqlite.transactionTemplates;
export const transactionTemplatesRelations = dialect === "postgresql" ? pg.transactionTemplatesRelations : sqlite.transactionTemplatesRelations;
export const transactions = dialect === "postgresql" ? pg.transactions : sqlite.transactions;
export const transactionsRelations = dialect === "postgresql" ? pg.transactionsRelations : sqlite.transactionsRelations;
export const transferSuggestions = dialect === "postgresql" ? pg.transferSuggestions : sqlite.transferSuggestions;
export const transfers = dialect === "postgresql" ? pg.transfers : sqlite.transfers;
export const transfersRelations = dialect === "postgresql" ? pg.transfersRelations : sqlite.transfersRelations;
export const usageAnalytics = dialect === "postgresql" ? pg.usageAnalytics : sqlite.usageAnalytics;
export const userHouseholdPreferences = dialect === "postgresql" ? pg.userHouseholdPreferences : sqlite.userHouseholdPreferences;
export const userSessions = dialect === "postgresql" ? pg.userSessions : sqlite.userSessions;
export const userSettings = dialect === "postgresql" ? pg.userSettings : sqlite.userSettings;
export const utilizationAlertState = dialect === "postgresql" ? pg.utilizationAlertState : sqlite.utilizationAlertState;

// Better Auth schema exports (used by a handful of admin/user endpoints)
export const betterAuthUser = dialect === "postgresql" ? pg.betterAuthUser : sqlite.betterAuthUser;
export const betterAuthSession = dialect === "postgresql" ? pg.betterAuthSession : sqlite.betterAuthSession;
export const betterAuthAccount = dialect === "postgresql" ? pg.betterAuthAccount : sqlite.betterAuthAccount;
export const betterAuthVerification =
  dialect === "postgresql" ? pg.betterAuthVerification : sqlite.betterAuthVerification;
